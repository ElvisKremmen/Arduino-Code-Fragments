void loop() {

    yourStr = padLeft (String (yourValue), yourNewLen);

}


String padLeft (String inString, int newLen) {    // Pad a numeric string with spaces for output
    while (inString.length() < newLen) {
    inString = String(" ") + inString; 
  };
  return inString;
}
